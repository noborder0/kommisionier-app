<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Kommissionier-App</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">Kommissionier-App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/orders">Aufträge</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user"></i> <%= user.name %>
                    </a>
                    <ul class="dropdown-menu">
                        <% if (user.role === 'admin') { %>
                            <li><a class="dropdown-item" href="/auth/register">Benutzer registrieren</a></li>
                            <li><hr class="dropdown-divider"></li>
                        <% } %>
                        <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    <!-- Flash-Nachrichten -->
    <% if(typeof success_msg !== 'undefined' && success_msg !== '') { %>
        <div class="alert alert-success alert-dismissible fade show">
            <%= success_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof error_msg !== 'undefined' && error_msg !== '') { %>
        <div class="alert alert-danger alert-dismissible fade show">
            <%= error_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof info_msg !== 'undefined' && info_msg !== '') { %>
        <div class="alert alert-info alert-dismissible fade show">
            <%= info_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof warning_msg !== 'undefined' && warning_msg !== '') { %>
        <div class="alert alert-warning alert-dismissible fade show">
            <%= warning_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <div class="card">
        <div class="card-header bg-primary text-white">
            <h2>Lieferschein suchen</h2>
        </div>
        <div class="card-body">
            <form action="/orders/search" method="POST">
                <div class="mb-3">
                    <label for="deliveryNoteNumber" class="form-label">Lieferscheinnummer</label>
                    <div class="input-group">
                        <input type="text" id="deliveryNoteNumber" name="deliveryNoteNumber" class="form-control" required>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-search"></i> Suchen
                        </button>
                    </div>
                    <small class="form-text text-muted">Geben Sie die Lieferscheinnummer ein oder scannen Sie den Barcode.</small>
                </div>
            </form>

            <div class="mt-4">
                <h4>Aktionen</h4>
                <div class="d-flex flex-wrap gap-2 mt-3">
                    <a href="/orders" class="btn btn-outline-primary">
                        <i class="fas fa-list"></i> Alle Aufträge
                    </a>
                    <a href="/orders?status=in_progress" class="btn btn-outline-warning">
                        <i class="fas fa-hourglass-half"></i> In Bearbeitung
                    </a>
                    <a href="/orders?status=completed" class="btn btn-outline-success">
                        <i class="fas fa-check-circle"></i> Abgeschlossen
                    </a>
                </div>
            </div>

            <div class="mt-5">
                <h4>Barcode-Scanner</h4>
                <div class="card bg-light">
                    <div class="card-body">
                        <p class="card-text">Sie können einen Barcode-Scanner verwenden, um Lieferscheinnummern schnell zu erfassen.</p>
                        <ol>
                            <li>Platzieren Sie den Cursor im Eingabefeld "Lieferscheinnummer"</li>
                            <li>Scannen Sie den Barcode auf dem Lieferschein</li>
                            <li>Das Formular wird automatisch abgesendet, wenn der Scanner eine Return-Taste sendet</li>
                        </ol>
                        <p class="mb-0"><strong>Tipp:</strong> Die meisten Scanner sind so konfiguriert, dass sie nach dem Scannen automatisch einen Return-Tastendruck senden.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card mt-4">
        <div class="card-header bg-light">
            <h4 class="mb-0">Synchronisierung mit Xentral</h4>
        </div>
        <div class="card-body">
            <p class="card-text">
                <i class="fas fa-info-circle text-primary"></i>
                Drücken Sie auf "Mit Xentral synchronisieren", um Lieferscheine aus Ihrem Xentral-System zu importieren.
                Bei einer großen Datenmenge können Sie die Synchronisierung einschränken.
            </p>

            <form action="/orders/sync" method="GET" class="mb-4">
                <div class="row g-3 align-items-center">
                    <div class="col-md-4">
                        <div class="form-group">
                            <label for="max" class="form-label">Max. Anzahl:</label>
                            <select class="form-select" id="max" name="max">
                                <option value="50">50 Lieferscheine</option>
                                <option value="100" selected>100 Lieferscheine</option>
                                <option value="200">200 Lieferscheine</option>
                                <option value="500">500 Lieferscheine</option>
                                <option value="1000">1.000 Lieferscheine</option>
                            </select>
                            <small class="form-text text-muted">Begrenze die Anzahl der zu synchronisierenden Lieferscheine.</small>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="form-group">
                            <label for="fromDate" class="form-label">Datum ab:</label>
                            <input type="date" class="form-control" id="fromDate" name="fromDate">
                            <small class="form-text text-muted">Nur Lieferscheine ab diesem Datum synchronisieren (Format: YYYY-MM-DD).</small>
                        </div>
                    </div>

                    <div class="col-md-4 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary w-100 py-2">
                            <i class="fas fa-sync"></i> Mit Xentral synchronisieren
                        </button>
                    </div>
                </div>
            </form>

            <div class="alert alert-info">
                <i class="fas fa-info-circle"></i> <strong>Info zur API-Filterung:</strong>
                Die Xentral-API filtert nach dem Dokumentendatum (documentDate) mit dem Operator "greaterThan".
                Standardmäßig werden Lieferscheine ab dem 1. Januar 2024 geladen. Die API-Anfragen verwenden eine
                Seitengröße von 50 Ergebnissen pro Seite (maximaler Wert laut API).
            </div>

            <div class="alert alert-warning">
                <i class="fas fa-exclamation-triangle"></i> <strong>Hinweis:</strong>
                Die Synchronisierung großer Datenmengen kann einige Zeit in Anspruch nehmen.
                Bei über 1.000 Lieferscheinen empfehlen wir die Verwendung der Filter, um die Belastung
                des Systems zu reduzieren.
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Barcode Scanner Logik -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const input = document.getElementById('deliveryNoteNumber');

        // Automatischer Fokus auf das Eingabefeld
        input.focus();

        // Scanner-Eingabe erkennen (schnelle Eingabe)
        let lastInputTime = 0;
        let buffer = '';
        const scanThreshold = 100; // ms zwischen Eingaben

        input.addEventListener('keypress', function(e) {
            const currentTime = new Date().getTime();

            if (currentTime - lastInputTime > scanThreshold) {
                buffer = '';
            }

            buffer += e.key;
            lastInputTime = currentTime;

            // Wenn Enter nach schneller Eingabe gedrückt wird, ist es wahrscheinlich ein Scan
            if (e.key === 'Enter' && buffer.length > 5 && currentTime - lastInputTime < scanThreshold) {
                e.preventDefault();
                this.form.submit();
            }
        });

        // Standard-Datum setzen (1. Januar 2024)
        const fromDateInput = document.getElementById('fromDate');
        if (fromDateInput) {
            fromDateInput.value = '2024-01-01';
        }
    });
</script>
</body>
</html>