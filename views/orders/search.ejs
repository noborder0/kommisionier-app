<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Kommissionier-App</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">Kommissionier-App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/orders">Aufträge</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user"></i> <%= user.name %>
                    </a>
                    <ul class="dropdown-menu">
                        <% if (user.role === 'admin') { %>
                            <li><a class="dropdown-item" href="/auth/register">Benutzer registrieren</a></li>
                            <li><hr class="dropdown-divider"></li>
                        <% } %>
                        <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    <!-- Flash-Nachrichten -->
    <% if(typeof success_msg !== 'undefined' && success_msg !== '') { %>
        <div class="alert alert-success alert-dismissible fade show">
            <%= success_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof error_msg !== 'undefined' && error_msg !== '') { %>
        <div class="alert alert-danger alert-dismissible fade show">
            <%= error_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof info_msg !== 'undefined' && info_msg !== '') { %>
        <div class="alert alert-info alert-dismissible fade show">
            <%= info_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof warning_msg !== 'undefined' && warning_msg !== '') { %>
        <div class="alert alert-warning alert-dismissible fade show">
            <%= warning_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <div class="card">
        <div class="card-header bg-primary text-white">
            <h2>Lieferschein suchen</h2>
        </div>
        <div class="card-body">
            <form action="/orders/search" method="POST">
                <div class="mb-3">
                    <label for="deliveryNoteNumber" class="form-label">Lieferscheinnummer</label>
                    <div class="input-group">
                        <input type="text" id="deliveryNoteNumber" name="deliveryNoteNumber" class="form-control" required>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-search"></i> Suchen
                        </button>
                    </div>
                    <small class="form-text text-muted">Geben Sie die Lieferscheinnummer ein oder scannen Sie den Barcode.</small>
                </div>
            </form>

            <div class="mt-4">
                <h4>Aktionen</h4>
                <div class="d-flex flex-wrap gap-2 mt-3">
                    <a href="/orders" class="btn btn-outline-primary">
                        <i class="fas fa-list"></i> Alle Aufträge
                    </a>
                    <a href="/orders?status=in_progress" class="btn btn-outline-warning">
                        <i class="fas fa-hourglass-half"></i> In Bearbeitung
                    </a>
                    <a href="/orders?status=completed" class="btn btn-outline-success">
                        <i class="fas fa-check-circle"></i> Abgeschlossen
                    </a>
                </div>
            </div>

            <div class="mt-5">
                <h4>Barcode-Scanner</h4>
                <div class="card bg-light">
                    <div class="card-body">
                        <p class="card-text">Sie können einen Barcode-Scanner verwenden, um Lieferscheinnummern schnell zu erfassen.</p>
                        <ol>
                            <li>Platzieren Sie den Cursor im Eingabefeld "Lieferscheinnummer"</li>
                            <li>Scannen Sie den Barcode auf dem Lieferschein</li>
                            <li>Das Formular wird automatisch abgesendet, wenn der Scanner eine Return-Taste sendet</li>
                        </ol>
                        <p class="mb-0"><strong>Tipp:</strong> Die meisten Scanner sind so konfiguriert, dass sie nach dem Scannen automatisch einen Return-Tastendruck senden.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card mt-4">
        <div class="card-header bg-light">
            <h4 class="mb-0">Synchronisierung mit No Border</h4>
        </div>
        <div class="card-body">
            <p class="card-text">
                <i class="fas fa-info-circle text-primary"></i>
                Drücken Sie auf "Mit No Border synchronisieren", um Lieferscheine aus Ihrem No Border-System zu importieren.
            </p>

            <div class="d-flex gap-2 mb-4">
                <a href="/orders/sync" class="btn btn-primary">
                    <i class="fas fa-sync"></i> Mit No Border synchronisieren
                </a>

                <a href="/orders/api-test" class="btn btn-outline-info">
                    <i class="fas fa-plug"></i> API-Verbindung testen
                </a>
            </div>

            <div class="alert alert-info">
                <i class="fas fa-info-circle"></i> <strong>Info zur Synchronisierung:</strong>
                Das System holt automatisch alle offenen Lieferscheine (Status: "in fulfillment") aus der No Border API
                und aktualisiert die lokale Datenbank entsprechend.
            </div>

            <div class="alert alert-success">
                <i class="fas fa-clock"></i> <strong>Automatische Synchronisierung:</strong>
                Die Synchronisierung läuft automatisch alle 1 Minute im Hintergrund.
                Neue Lieferscheine werden automatisch importiert.
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Barcode Scanner Logik -->
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const input = document.getElementById('deliveryNoteNumber');

        // Automatischer Fokus auf das Eingabefeld
        input.focus();

        // Scanner-Eingabe erkennen (schnelle Eingabe)
        let lastInputTime = 0;
        let buffer = '';
        const scanThreshold = 100; // ms zwischen Eingaben

        input.addEventListener('keypress', function(e) {
            const currentTime = new Date().getTime();

            if (currentTime - lastInputTime > scanThreshold) {
                buffer = '';
            }

            buffer += e.key;
            lastInputTime = currentTime;

            // Wenn Enter nach schneller Eingabe gedrückt wird, ist es wahrscheinlich ein Scan
            if (e.key === 'Enter' && buffer.length > 5 && currentTime - lastInputTime < scanThreshold) {
                e.preventDefault();
                this.form.submit();
            }
        });
    });
</script>
</body>
</html>