<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Kommissionier-App</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<%- include('partials/navbar') %>

<div class="container mt-4">
    <!-- Flash-Nachrichten -->
    <% if(typeof success_msg !== 'undefined' && success_msg !== '') { %>
        <div class="alert alert-success alert-dismissible fade show">
            <%= success_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <% if(typeof error_msg !== 'undefined' && error_msg !== '') { %>
        <div class="alert alert-danger alert-dismissible fade show">
            <%= error_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <!-- Willkommen Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card bg-primary text-white">
                <div class="card-body">
                    <div class="row align-items-center">
                        <div class="col-md-8">
                            <h2 class="mb-1">
                                <i class="fas fa-tachometer-alt"></i>
                                Willkommen, <%= user.name %>!
                            </h2>
                            <p class="mb-0 lead">
                                Kommissionier- und Warenausgangssystem
                            </p>
                        </div>
                        <div class="col-md-4 text-end">
                            <div class="text-white-50">
                                <%= new Date().toLocaleDateString('de-DE', {
                                    weekday: 'long',
                                    year: 'numeric',
                                    month: 'long',
                                    day: 'numeric'
                                }) %>
                            </div>
                            <div class="h4 mb-0">
                                <%= new Date().toLocaleTimeString('de-DE', {
                                    hour: '2-digit',
                                    minute: '2-digit'
                                }) %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Hauptfunktionen -->
    <div class="row">
        <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <i class="fas fa-search fa-3x text-primary"></i>
                    </div>
                    <h4 class="card-title">Lieferschein suchen</h4>
                    <p class="card-text">
                        Suchen Sie einen Lieferschein nach Nummer oder scannen Sie den Barcode.
                    </p>
                    <a href="/orders/search" class="btn btn-primary btn-lg">
                        <i class="fas fa-search"></i> Zur Suche
                    </a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <i class="fas fa-clipboard-list fa-3x text-success"></i>
                    </div>
                    <h4 class="card-title">Aktuelle Aufträge</h4>
                    <p class="card-text">
                        Zeigen Sie alle aktuellen Aufträge und deren Status an.
                    </p>
                    <a href="/orders" class="btn btn-success btn-lg">
                        <i class="fas fa-list"></i> Aufträge anzeigen
                    </a>
                </div>
            </div>
        </div>

        <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100 shadow-sm border-0">
                <div class="card-body text-center">
                    <div class="mb-3">
                        <i class="fas fa-hourglass-half fa-3x text-warning"></i>
                    </div>
                    <h4 class="card-title">In Bearbeitung</h4>
                    <p class="card-text">
                        Zeigen Sie alle Aufträge an, die gerade kommissioniert werden.
                    </p>
                    <a href="/orders?status=in_progress" class="btn btn-warning btn-lg">
                        <i class="fas fa-play"></i> Fortsetzen
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Schnellzugriffe -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-light">
                    <h4 class="mb-0"><i class="fas fa-bolt"></i> Schnellzugriffe</h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-lg-3 col-md-6 mb-3">
                            <a href="/orders?status=new" class="btn btn-outline-secondary w-100">
                                <i class="fas fa-plus-circle"></i><br>
                                <small>Neue Aufträge</small>
                            </a>
                        </div>
                        <div class="col-lg-3 col-md-6 mb-3">
                            <a href="/orders?status=packed" class="btn btn-outline-info w-100">
                                <i class="fas fa-box"></i><br>
                                <small>Verpackt</small>
                            </a>
                        </div>
                        <div class="col-lg-3 col-md-6 mb-3">
                            <a href="/orders?status=shipped" class="btn btn-outline-primary w-100">
                                <i class="fas fa-truck"></i><br>
                                <small>Versendet</small>
                            </a>
                        </div>
                        <div class="col-lg-3 col-md-6 mb-3">
                            <a href="/orders?status=completed" class="btn btn-outline-success w-100">
                                <i class="fas fa-check-circle"></i><br>
                                <small>Abgeschlossen</small>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Sync-Status für alle Benutzer (vereinfacht) -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card bg-light">
                <div class="card-body">
                    <div class="row align-items-center">
                        <div class="col-md-8">
                            <h6 class="mb-1">
                                <i class="fas fa-sync text-primary"></i>
                                Automatische Synchronisierung mit No Border
                            </h6>
                            <p class="mb-0 text-muted small">
                                Neue Lieferscheine werden automatisch alle 1 Minute importiert.
                            </p>
                        </div>
                        <div class="col-md-4 text-end">
                            <span class="badge bg-success">
                                <i class="fas fa-check-circle"></i> Aktiv
                            </span>
                            <% if(user.role === 'admin') { %>
                                <a href="/settings" class="btn btn-sm btn-outline-primary ms-2">
                                    <i class="fas fa-cogs"></i> Verwalten
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Hilfe und Tipps -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card border-info">
                <div class="card-header bg-info text-white">
                    <h6 class="mb-0"><i class="fas fa-lightbulb"></i> Tipp des Tages</h6>
                </div>
                <div class="card-body">
                    <p class="mb-2">
                        <strong>Barcode-Scanner verwenden:</strong>
                        Sie können einen Barcode-Scanner verwenden, um Lieferscheinnummern schnell zu erfassen.
                        Einfach das Eingabefeld fokussieren und den Barcode scannen.
                    </p>
                    <small class="text-muted">
                        <i class="fas fa-keyboard"></i> Tastaturbefehle: Strg+F für Schnellsuche, Strg+L für Auftragsliste
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Tastaturbefehle -->
<script>
    document.addEventListener('keydown', function(e) {
        // Strg+F für Schnellsuche
        if (e.ctrlKey && e.key === 'f') {
            e.preventDefault();
            window.location.href = '/orders/search';
        }

        // Strg+L für Auftragsliste
        if (e.ctrlKey && e.key === 'l') {
            e.preventDefault();
            window.location.href = '/orders';
        }
    });

    // Auto-refresh der Uhrzeit
    setInterval(function() {
        const now = new Date();
        const timeElement = document.querySelector('.h4.mb-0');
        if (timeElement) {
            timeElement.textContent = now.toLocaleTimeString('de-DE', {
                hour: '2-digit',
                minute: '2-digit'
            });
        }
    }, 60000); // Jede Minute aktualisieren
</script>
</body>
</html>